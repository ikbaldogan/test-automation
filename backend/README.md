To install dependencies, run npm install command in the backend folder
if not installed run the following commands,
    npm install body-parser
    npm install cors
    npm install express
then start the backend with node server.js command